#[cfg(test)]
mod multiple_column_same_types_tests {

    /// These tests check that calling greatest on multiple columns with the same type
    ///
    /// This only check the greatest logic
    ///
    ///
    /// TODO - TEST GREATEST ON SAME COLUMN MULTIPLE GREATEST AND DIFFERENT COLUMNS MULTIPLE GREATEST

    #[tokio::test]
    async fn test_invalid_expr() {
        todo!();
    }
}
